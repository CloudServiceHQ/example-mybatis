<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.dao.TodoListRepository">
    <select id="getAllTodoList" resultType="com.example.demo.dto.TodoListDto">
        SELECT * FROM todo_list;
    </select>
</mapper>